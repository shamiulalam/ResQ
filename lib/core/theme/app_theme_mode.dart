/// The two theme modes available in ResQ.
///
/// • [light]   — bright white/cream palette, suitable for daytime use.
/// • [dark] — the near-black and teal Pet Match palette.
enum AppThemeMode {
  light,
  dark,
}
